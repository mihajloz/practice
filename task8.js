// Create a function that takes a string as input and returns the most common character in the string.

// Find the most common character in a string:
// Input: "Hello, world!"
// Output: "l" (as "l" appears the most)
